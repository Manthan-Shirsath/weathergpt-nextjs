// This file is intentionally kept as a thin re-export wrapper for backward
// compatibility. The actual implementation has moved to ToolResultRenderer.tsx
// which uses the AI SDK v7 UIMessage parts API.
export { ToolResultRenderer as ToolInvocation } from './ToolResultRenderer';
